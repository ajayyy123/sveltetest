import cookie from 'cookie'
// // import storage tabe
// // var cdata={"user":"Ajay"}
// export async function handle({ request, resolve }) {
//     // const cookies =cookie.parse(resolve.cookie) 
//     // var cdata=resolve
//     // console.log(cookies)
// }
export async function getSession(request,resolve){
    let user=request.headers
   
//    return cdata
return {
    // 
    user 
}
 
}